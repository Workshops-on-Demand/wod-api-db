http {
	# Hide Nginx version
	server_tokens off;
	
	# Security headers
	add_header X-Frame-Options "SAMEORIGIN" always;
	add_header X-XSS-Protection "1; mode=block" always;
	add_header X-Content-Type-Options "nosniff" always;
	add_header Referrer-Policy "no-referrer-when-downgrade" always;
	add_header Content-Security-Policy "default-src 'self' http: https: data: blob: 'unsafe-inline'" always;

	# Disable server signature
	server_tokens off;

	# Limit request size
	client_max_body_size 10M;

	# Timeout settings
	client_body_timeout 12;
	client_header_timeout 12;
	keepalive_timeout 15;
	send_timeout 10;
}

server {
        location / {
                proxy_pass http://{{ WODAPIDBFQDN }}:{{ WODAPIDBPORT }};
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection 'upgrade';
                proxy_set_header Host $host;
        	proxy_set_header X-Real-IP $remote_addr;
        	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        	proxy_set_header X-Forwarded-Proto $scheme;
                proxy_cache_bypass $http_upgrade;
        }
	server_name {{ WODAPIDBFQDN }};
	listen 443 ssl; # managed by Certbot
        listen [::]:443 default_server;
	ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt; # managed by Certbot
	ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key; # managed by Certbot
}

server {
        location / {
                proxy_pass http://{{ WODFRONTENDFQDN }}:{{ WODFRONTENDPORT }};
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection 'upgrade';
                proxy_set_header Host $host;
        	proxy_set_header X-Real-IP $remote_addr;
        	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        	proxy_set_header X-Forwarded-Proto $scheme;
                proxy_cache_bypass $http_upgrade;
        }
	server_name {{ WODFRONTENDFQDN }};
	listen 443 ssl; # managed by Certbot
        listen [::]:443 default_server;
	ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt; # managed by Certbot
	ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key; # managed by Certbot
}

server {
        location / {
                proxy_pass http://{{ WODBACKENDFQDN }}:{{ WODBACKENDPORT }};
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection 'upgrade';
                proxy_set_header Host $host;
        	proxy_set_header X-Real-IP $remote_addr;
        	proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        	proxy_set_header X-Forwarded-Proto $scheme;
                proxy_cache_bypass $http_upgrade;
        }
	server_name {{ WODBACKENDFQDN }};
	listen 443 ssl; # managed by Certbot
        listen [::]:443 default_server;
	ssl_certificate /etc/ssl/certs/nginx-selfsigned.crt; # managed by Certbot
	ssl_certificate_key /etc/ssl/private/nginx-selfsigned.key; # managed by Certbot
}
